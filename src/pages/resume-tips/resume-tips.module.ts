import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { ResumeTipsPage } from './resume-tips';

@NgModule({
  declarations: [
    ResumeTipsPage,
  ],
  imports: [
    IonicPageModule.forChild(ResumeTipsPage),
  ],
})
export class ForumPageModule {}
